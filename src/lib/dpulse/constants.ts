import { DPULSE_CONFIG } from './config';

export { DPULSE_CONFIG };

export const PEER_WAIT_TIMEOUT_MS = 20000;
export const PEER_RETRY_BASE_DELAY_MS = 1000;
export const PEER_RETRY_MAX_DELAY_MS = 30000;
export const STORE_HISTORY_HOURS = 1;
export const RETRY_DELAY_MS = 5000;
